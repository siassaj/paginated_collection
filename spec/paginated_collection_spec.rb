require 'spec_helper'

describe PaginatedCollection do
  it 'has a version number' do
    expect(PaginatedCollection::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
